import React from "react";
import { Carousel } from "react-bootstrap";

function ImgCarouselItem({ title, subtitle, imgSrc, interval }) {
  console.log(
    `ImgCarouselItem ${title} - ${subtitle} - ${imgSrc} - ${interval}`
  );
  return <h1></h1>;
}

export default ImgCarouselItem;
